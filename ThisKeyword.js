/**
 * this in global space
 * this inside function
 * this in strict mode (this substituation)
 * this value depends on how this is called
 * this inside object method
 * call apply bind method (sharing methods)
 * this inside arrow function
 * this inside nested arrow function
 * this inside DOM
 */
/*------------------------------------------------------------------------------------------------*/ 
//This keyword 
// this in global scope
  //this in global scope will give window object 
      //Js runs everywhere not only inside browser it can run inside mobile,it can run inside node
      // in node this is global
      //in browser this is window

      
